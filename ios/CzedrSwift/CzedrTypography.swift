//
//  CzedrTypography.swift
//  Larger default text for sign-in, payments, and PIN (accessibility-friendly).
//

import SwiftUI

enum CzedrTypography {
    /// Applied app-wide via environment on the SwiftUI root.
    static let contentSizeCategory: ContentSizeCategory = .accessibilityLarge
}
