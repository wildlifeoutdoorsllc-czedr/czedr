-- Local Czedr MySQL: single application database (ledger-only API).
-- Run: mysql -u root < scripts/local-mysql-init.sql

CREATE DATABASE IF NOT EXISTS saturn CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

SHOW DATABASES;
