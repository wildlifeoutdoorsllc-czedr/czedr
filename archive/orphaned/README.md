# Archived / orphaned assets

Files moved here were **not referenced** by the current Xcode target (or were duplicate vendor trees) but are kept for reference or local history.

| Path | Reason |
|------|--------|
| `SDWebImage/` | Duplicate of `RNCrypt/SDWebImage/` (the target builds from `RNCrypt/`). |
| `scripts/rename-databases-to-planets.sql` | Historical planet DB rename notes only; product is Saturn-only. |
| `root-assets/Splash-Screen@2x.png` | Not listed in `payooxe.xcodeproj`; app uses `images/splash*.png`. |
| `external-docs/` | Local business PDFs / distribution docs (not part of the app build). |

Restore a file by moving it back to its original path and `git add` as needed.
